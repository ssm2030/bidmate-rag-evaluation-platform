# 시나리오 A 실험 결과 보고서: P1_C1_F1_S1_T1

## 1. 실험 개요
- **실험 ID**: `p1`
- **프롬프트 설정**: `prompt_P1_C1_F1_S1_T1.yaml` (기본 베이스라인)
- **임베딩 모델**: BAAI/bge-m3
- **LLM 모델**: google/gemma-4-E4B-it (4-bit quantization)
- **청킹 전략**: 1000자 / 150 오버랩
- **테스트셋**: `eval_batch_41_rank_1-20.csv` (20개 샘플)

## 2. 주요 지표 요약

### 검색 성능 (Retrieval)
| 지표 | 결과 | 비고 |
| --- | --- | --- |
| Hit Rate@5 | **1.0000** | 모든 문제에서 관련 문서 검색 성공 |
| MRR | **1.0000** | 정답 문서가 항상 상위에 랭크됨 |
| nDCG@5 | **1.0000** | 검색 순위 최적화 완벽 |

### 생성 품질 (LLM Judge)
| 지표 | 결과 | 분석 |
| --- | --- | --- |
| Faithfulness | **0.6875** | "없음" 답변 시 판정 모델이 0점 처리하는 경향 발견 |
| Answer Relevance | **0.7750** | 질문에 대한 직접적인 답이 부족하다고 판단됨 (섹션 분리 영향) |
| Context Precision | **0.7000** | 검색된 문맥 활용도 적정 수준 |
| Context Recall | **0.7375** | 정답 도출에 필요한 정보 포함 여부 양호 |

### 운영 지표 (Ops)
- **평균 응답 속도**: 54,152ms (약 54초)
- **평균 토큰 사용량**: 4,920 tokens
- **총 Judge 비용**: $0.0168 (gpt-4o-mini 사용)

## 3. 심층 분석 및 인사이트

### 저조한 품질 지표의 원인 (Low Score Analysis)
1. **정직한 "없음" 답변의 역효과**:
   - `Q009` 사례: 모델이 문서에 없는 하드웨어 사양에 대해 "찾을 수 없다"고 정확히 답변했으나, 판정 모델은 "정보를 포함하지 않았다"는 이유로 0점(Faithfulness, Relevance)을 부여함. 이는 RAG 성능 저하가 아닌 평가 모델의 엄격한 기준에 의한 현상임.
2. **구조화된 답변 형식의 영향**:
   - 프롬프트 지침에 따라 `핵심 답변`, `근거`, `문서에 없는 항목` 섹션을 나누어 답변함. 판정 모델은 답변이 파편화되어 있다고 느껴 `Answer Relevance` 점수를 보수적으로 측정함.
3. **P-C-F-S-T 프레임워크 준수 확인**:
   - 페르소나와 제약 사항(한국어 사용, 인용 번호 표기)은 매우 잘 지켜짐. 특히 할루시네이션(환각) 없이 근거가 부족할 때 솔직하게 답변하는 안정성을 보임.

## 4. 향후 실험 방향
- **P2 실험**: 조금 더 유연하거나 요약된 스타일의 프롬프트를 사용하여 판정 모델의 점수 변화 확인.
- **판정 모델 가이드**: "문서에 근거가 없어 없다고 답한 경우"를 정답으로 인정하도록 판정 프롬프트를 보완할 필요가 있음.
- **인용 강화**: 답변 내 인용 번호(`[n]`)가 판정 점수에 미치는 영향 추가 분석 예정.
